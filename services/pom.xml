<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cn.huntercat</groupId>
    <artifactId>lieshoucloud-services-open</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>LieShouCloud Open · Services Parent</name>
    <description>LieShouCloud open-source core services parent</description>

    <modules>
        <module>common</module>
    </modules>

    <properties>
        <!-- ===== Java ===== -->
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <maven.compiler.parameters>true</maven.compiler.parameters>

        <!-- ===== Spring 栈 ===== -->
        <spring-boot.version>3.5.7</spring-boot.version>
        <spring-cloud.version>2023.0.5</spring-cloud.version>
        <spring-cloud-alibaba.version>2023.0.1.0</spring-cloud-alibaba.version>

        <!-- ===== 工程规约工具 ===== -->
        <spotless.version>2.43.0</spotless.version>
        <jacoco.version>0.8.12</jacoco.version>

        <!-- ===== OpenAPI / SpringDoc（Phase 5 跨端契约） ===== -->
        <springdoc.version>2.8.17</springdoc.version>

        <!-- ===== Spring Security / JWT（Phase 5 闭环鉴权） ===== -->
        <jjwt.version>0.12.6</jjwt.version>

        <!-- ===== 测试 ===== -->
        <h2.version>2.2.224</h2.version>

        <!-- ===== Testcontainers：覆盖 spring-boot BOM（1.21.3）
             升 1.21.4 —— 修复 Docker Desktop socket 代理 400：docker-java 3.4.0 发空 Host header
             被代理拒绝（BadRequest 400 / Could not find a valid Docker environment），3.4.1 起修复
             注意：仅设属性无效（Maven 3.9 下 BOM 内部属性优先），必须把 testcontainers-bom
             显式 import 到 dependencyManagement 最前（先声明者胜） ===== -->
        <testcontainers.version>1.21.4</testcontainers.version>

        <!-- ===== CSV 导入（数据导入工具 · Phase 9） ===== -->
        <commons-csv.version>1.12.0</commons-csv.version>

        <!-- ===== 阿里云短信（dysmsapi · 验证码通道 · Phase 2） ===== -->
        <aliyun.dysmsapi.version>4.1.1</aliyun.dysmsapi.version>

        <!-- ===== Project ===== -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <!-- ============================================================
         集中管理所有 BOM，子模块不再重复
         ============================================================ -->
    <dependencyManagement>
        <dependencies>
            <!-- Testcontainers BOM：**必须排在最前**（Maven dependencyManagement 先声明者胜）
                 覆盖 spring-boot 管理的 1.21.3 → 1.21.4（修复 Docker Desktop socket 代理空 Host 400）
                 仅设属性无效（Maven 3.9 下 BOM 内部属性优先于外部覆盖） -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud Alibaba BOM -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- H2 in-memory DB for @DataJpaTest -->
            <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>${h2.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- Apache Commons CSV（CSV 导入解析） -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-csv</artifactId>
                <version>${commons-csv.version}</version>
            </dependency>
            <!-- 阿里云短信 SDK（验证码发送 · ADR-0023 Phase 2） -->
            <dependency>
                <groupId>com.aliyun</groupId>
                <artifactId>dysmsapi20170525</artifactId>
                <version>${aliyun.dysmsapi.version}</version>
            </dependency>
            <!-- SpringDoc OpenAPI 统一版本 -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <!-- jjwt 共享版本 -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- 底座共享模块（L2-1 · Bottom-Up）：统一异常体契约 -->
            <dependency>
                <groupId>cn.huntercat</groupId>
                <artifactId>lieshoucloud-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- JWT 共享模块（HS256 单一事实源 · auth 签发 / gateway 验证） -->
            <dependency>
                <groupId>cn.huntercat</groupId>
                <artifactId>lieshoucloud-jwt-support</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Spring Boot Maven Plugin 由子模块自己用 -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>${maven.compiler.parameters}</parameters>
                    </configuration>
                </plugin>

                <!-- ============================================================
                     Spotless: Java 端代码格式化与导入顺序
                     ============================================================ -->
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless.version}</version>
                    <configuration>
                        <java>
                            <googleJavaFormat>
                                <style>GOOGLE</style>
                            </googleJavaFormat>
                            <removeUnusedImports/>
                            <importOrder>
                                <order>org.springframework,org.springframework.cloud,com.alibaba.cloud,jakarta,org,com,lombok,\#</order>
                            </importOrder>
                        </java>
                    </configuration>
                </plugin>

                <!-- ============================================================
                     JaCoCo: 后端测试覆盖率收集 + threshold check
                     起步期：BUNDLE LINE ≥ 40%（CI 报告但不阻断；Maven 配置中不开启 rule 强制）
                     成熟期：将 minimum 提到 0.60，并把 rule 放到 execution 强制
                     ============================================================ -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <configuration>
                        <includes>
                            <include>cn/huntercat/lieshoucloudpro/**/*.class</include>
                        </includes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!--
              激活 spotless：所有子模块在 `mvn verify` 时自动跑 `spotless:check`。
              失败则中断 verify（PR 不能合）。本地开发用 `mvn spotless:apply` 自动修。
            -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <phase>verify</phase>
                    </execution>
                </executions>
            </plugin>

            <!--
              激活 JaCoCo：所有子模块在 `mvn test` 阶段 prepare-agent 收集覆盖率，
              `mvn verify` 阶段生成 report。
              child <jacoco:report> 默认输出 target/site/jacoco/index.html
              并写 target/jacoco.exec（默认）
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>default-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
